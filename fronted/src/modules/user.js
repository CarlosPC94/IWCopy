export default class User {
    constructor(name,email,password,birthdate,deliveryAddress) {
      this.name = name,
      this.email = email;
      this.password=password;
      this.birthdate=birthdate;
      this.deliveryAddress=deliveryAddress

    }
  }